// ১️⃣ JSON ডাটা পার্স করার জন্য dart:convert ইমপোর্ট করা হচ্ছে
import 'dart:convert';

// ২️⃣ HTTP অনুরোধ (GET, POST ইত্যাদি) পাঠানোর জন্য http প্যাকেজ ইমপোর্ট
import 'package:http/http.dart' as http;

// ৩️⃣ ProductModel ক্লাস (ডাটা মডেল) ফাইল থেকে ইমপোর্ট
import 'model/product_model.dart';

// ৪️⃣ সব API লিংক (baseUrl, endpoints) রাখা utils/url.dart ফাইল ইমপোর্ট
import 'utils/url.dart';


// 🔸 ProductApiController নামের একটা ক্লাস বানানো হয়েছে
class ProductApiController {

  // ৫️⃣ এখানে ProductData টাইপের একটা লিস্ট রাখা হচ্ছে — সার্ভার থেকে পাওয়া ডাটা এতে সেভ হবে
  List<ProductData> productData = [];


  // 🔹 সার্ভার থেকে প্রোডাক্ট ডাটা আনার জন্য একটা Future ফাংশন তৈরি করা হলো
  Future fetchProduct() async {

    // ৬️⃣ সার্ভারে HTTP GET রিকোয়েস্ট পাঠানো হচ্ছে — UrlAll.readProduct হলো Read API endpoint
    final response = await http.get(Uri.parse(UrlAll.readProduct));

    // ৭️⃣ যদি সার্ভার সঠিকভাবে (statusCode 200) রেসপন্স দেয়
    if (response.statusCode == 200) {

      // ৮️⃣ সার্ভার থেকে পাওয়া JSON string কে map আকারে রূপান্তর (decode) করা হচ্ছে
      final data = jsonDecode(response.body);

      // ৯️⃣ সেই map ডাটা ProductModel ক্লাসে পাঠিয়ে model object বানানো হচ্ছে
      final productModel = ProductModel.fromJson(data);

      // 🔟 ProductModel থেকে data লিস্টটা productData তে সেভ করা হচ্ছে
      // যদি null হয়, তাহলে খালি লিস্ট ([]).
      productData = productModel.data ?? [];

    } else {
      // ১১️⃣ যদি সার্ভার ২০০ না দেয়, তাহলে error throw করবে
      throw Exception("Failed to load products");
    }
  }
}
